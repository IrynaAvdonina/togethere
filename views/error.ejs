<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Помилка</title>
  <style>
    body {
      font-family: Arial, sans-serif;
    }

    .container {
      text-align: center;
      margin-top: 50px;
    }

    h1 {
      color: red;
    }

    p {
      font-size: 18px;
    }

    a {
      color: orangered
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>Помилка <%= status %> <span class="emoji">😞</h1>
    <% if (status=='404') { %>
    <p>На жаль, ми не змогли знайти сторінку, яку ви шукаєте.</p>
    <% }else if(status>=500){ %>
    <p>Внутрішня помилка сервера. Спробуйте пізніше.</p>
    <% }else if(status>=400){ %>
    <p>Ваш запит не може бути оброблений через некоректні дані. Перевірте введену інформацію та спробуйте ще раз.</p>
    <% } %>
    <a href="/">Повернутися на головну сторінку</a>
  </div>
</body>

</html>